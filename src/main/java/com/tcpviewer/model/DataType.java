package com.tcpviewer.model;

/**
 * Enum representing the type of data captured from TCP connections.
 */
public enum DataType {
    /**
     * Text data (UTF-8, ASCII, etc.)
     */
    TEXT,

    /**
     * Binary data
     */
    BINARY
}
