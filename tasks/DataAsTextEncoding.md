**Kind (Bug, Feature)

Feature

**Short Description

Encodung data always as Test

**Text

Change the representation of the sent and received data in the data view as follows:

* Represent the data always as text. Remove the hexdump representation
* Use always the ISO_8859_1 encoding.
* Represent the byte values missing in the  ISO_8859_1 encoding as ?
* 
**Status (Open, Done, Canceled)

Done