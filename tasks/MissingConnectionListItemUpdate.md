**Kind (Bug, Feature)

Bug

**Short Description

No update by connection changes

**Text

When the status of a connection changes, like for example if a connection gets closed, the corresponding list item in the GUI gets no update. Only after the user selects the item does an update occur.

**Status (Open, Done, Canceled)

Done.